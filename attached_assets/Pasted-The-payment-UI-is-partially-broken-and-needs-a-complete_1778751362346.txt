The payment UI is partially broken and needs a complete functional fix for mobile browsers and UPI apps.

Current Issues:
1. The “Pay Now / Open UPI App” button is missing.
2. Large blank space appears inside the payment card.
3. “I have completed payment” is active before payment starts.
4. No QR fallback exists.
5. No expired-session handling.
6. UPI deep linking is unreliable on mobile browsers.
7. Payment flow UX is confusing.

Please inspect the entire payment page, components, CSS, routing, and UPI logic and fix everything properly.

Required Functional Fixes:

1. Add a primary CTA button:
   - “Pay ₹X via UPI”
   - Must open GPay, PhonePe, Paytm, BHIM, or any UPI app using UPI intent.

2. Generate proper UPI link:
   Example:
   upi://pay?pa=merchant@upi&pn=Bitebend&am=1.05&cu=INR

3. Add fallback options:
   - Show QR code
   - Copy UPI ID button
   - Retry payment button

4. Fix blank space issue:
   - Inspect hidden components
   - Inspect conditional rendering
   - Inspect CSS height/display issues
   - Ensure payment widget renders properly on mobile devices

5. Improve payment flow order:
   Order Summary
   Amount
   Pay Now Button
   Supported UPI Apps
   QR Code
   Timer
   “I Have Paid” Button

6. Disable “I have completed payment” until payment button is clicked.

7. Add session expiry handling:
   - Disable payment after timer ends
   - Show “Session Expired”
   - Prevent confirmation after expiry

8. Add loading/error states:
   - Loading spinner while redirecting
   - Error message if UPI app fails to open
   - Retry option

9. Ensure compatibility with:
   - Android Chrome
   - Google Lens browser
   - In-app browsers
   - Low-end mobile devices

10. Check routing and deployment:
   - Fix blank page issue after QR scan
   - Ensure SPA routing works correctly
   - Verify HTTPS and domain redirects
   - Ensure page loads directly from QR scans

11. Improve UI spacing and responsiveness:
   - Remove large empty white space
   - Make buttons properly visible
   - Improve mobile layout

After fixing, provide:
- Which files were changed
- Why the issue happened
- Exact fixes implemented
- Any remaining limitations