URGENT: Personal UPI payment still fails in Paytm with:

"We've encountered a technical glitch"

Screenshot attached.

This is no longer a UI issue. Please do a full end-to-end repair of UPI request generation and app-launch logic.

Observed:

Paytm opens
Restaurant appears
Amount appears as:

₹2.1

Then Paytm rejects request with technical glitch.

==================================================
LIKELY ROOT CAUSES
==================

1. Amount formatting

Current:

2.1

Required:

2.10

Use:

const amount = Number(orderTotal).toFixed(2)

UPI apps can reject malformed decimal values.

==================================================
2. Remove intent:// manipulation for Paytm

Current implementation uses:

intent://pay?...#Intent;package=net.one97.paytm;end

This is fragile.

For payment apps:

Use ONLY:

upi://pay?...

and allow Android system chooser.

Reason:

Paytm may reject package-routed intent URLs.

GPay and PhonePe are more tolerant.

==================================================
3. Log COMPLETE generated URL

Add:

console.log("[UPI FULL URL]", upiLink)

Copy exact output.

Expected:

upi://pay?
pa=restaurant@okaxis
&pn=Olive%20Garden
&am=2.10
&cu=INR
&tr=BITEBN42
&tn=Order42

==================================================
4. Remove all special chars entirely

Apply sanitizeUPIText()

to:

pn
tn
tr

Verify output:

No:

# ? & = % : ; / \

==================================================
5. Verify UPI ID validity

Log:

upiId

Confirm:

restaurant@okaxis

No spaces
No hidden characters
No trailing whitespace

Use:

upiId.trim()

==================================================
6. Test matrix

Test exact same URL:

* Google Pay
* PhonePe
* Paytm

Compare behavior.

If Paytm only fails:

Fallback:

Do not deep-link into Paytm package.

Use Android chooser instead.

==================================================
AFTER FIX PROVIDE

* generated URL
* before/after examples
* exact root cause
* files changed
* Paytm test confirmation
