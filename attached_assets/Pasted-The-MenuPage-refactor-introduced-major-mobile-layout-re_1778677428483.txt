The MenuPage refactor introduced major mobile layout regressions.

The app loads correctly now, but UI structure is broken on mobile browsers.

Visible issues from the screenshot:

* restaurant title overflowing outside header
* search icon/input overlapping
* menu card spacing collapsed
* image layout broken
* bottom legend overlapping
* inconsistent widths/padding
* components no longer fitting viewport properly

IMPORTANT:
This is a mobile responsive layout regression introduced during component extraction/refactor.

Please fully audit and restore the original mobile-first layout behavior.

==================================================
CHECK THESE AREAS
=================

1. Lost wrapper/container classes
   Compare old MenuPage layout wrappers vs extracted components:

* max-width
* w-full
* min-w-0
* overflow-hidden
* flex-col
* gap/padding/margin
* rounded containers

2. Flex/grid regressions
   Check:

* missing flex-shrink-0
* missing flex-1
* broken items-start/items-center
* incorrect grid columns
* image/text container widths

3. Sticky header/search area
   Search bar and category tabs likely lost:

* spacing
* z-index
* sticky container layout
* responsive sizing

4. Menu item cards
   Restore:

* proper image aspect ratio
* card padding
* vertical spacing
* responsive image sizing
* text wrapping
* button alignment

5. Restaurant header
   Title must:

* wrap safely
* not overflow container
* support long restaurant names
* preserve padding

6. Footer legend overlap
   Veg/Non-Veg legend should:

* have spacing from cards
* not overlap viewport bottom
* account for fixed cart bar

==================================================
REQUIRED FIX APPROACH
=====================

* Compare extracted components against pre-refactor layout
* Restore original responsive Tailwind classes exactly where lost
* Test on narrow Android viewport widths
* Prioritize mobile-first layout

==================================================
VERIFY ON
=========

* Android Chrome
* Google Lens browser
* Samsung Internet
* 320px width
* 360px width
* long restaurant names
* large images
* empty descriptions

==================================================
IMPORTANT
=========

Do NOT redesign the UI.
Restore the original stable mobile layout and spacing behavior while keeping the new component architecture.
