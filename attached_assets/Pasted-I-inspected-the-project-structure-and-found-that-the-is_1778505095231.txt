I inspected the project structure and found that the issue is most likely happening inside the Portal frontend (`artifacts/portal`) during the initial React boot + auth check process.

The app currently shows the inline loader:

“Loading Bitebend Portal…”

but React is either:

* failing before hydration,
* failing to load chunks on mobile browsers,
* OR getting stuck during `/api/auth/me` initialization.

I also noticed:

* heavy lazy-loaded chunk splitting,
* legacy mobile compatibility handling,
* auth refresh dependency on `/api/auth/me`,
* and a custom loading overlay system.

The blank page issue in Google Lens/mobile browser strongly indicates:

1. JS bundle crash,
2. chunk loading failure,
3. unsupported mobile browser syntax,
4. service worker cache corruption,
5. or BASE_PATH/domain mismatch after deployment.

Please do a FULL PROJECT-WIDE DEBUG instead of isolated fixes.

Mandatory checks:

* Inspect ALL frontend console errors
* Inspect ALL network failures
* Verify chunks load correctly on mobile
* Verify `/api/auth/me` resolves correctly
* Verify production build paths
* Verify Vite asset URLs after deployment
* Verify legacy bundle compatibility
* Verify no infinite Suspense fallback loop
* Verify no service worker cached old assets
* Verify custom domain routing + HTTPS
* Verify React app hydrates successfully on Android browsers

Important project areas to inspect:

* `artifacts/portal/src/App.tsx`
* `artifacts/portal/src/main.tsx`
* `artifacts/portal/src/contexts/AuthContext.tsx`
* `artifacts/portal/src/lib/api.ts`
* `artifacts/portal/vite.config.ts`
* deployment/reverse proxy configuration
* static asset/chunk serving configuration

Please implement an ACTUAL FIX and not temporary retries.

After completion, provide:

1. Which files were changed
2. Root cause of the issue
3. Exact fixes implemented
4. Why mobile browsers were failing
5. What was optimized for performance/stability
6. Confirmation that custom domain works correctly now

Also ensure:

* if API fails, app still opens login page,
* no endless spinner remains,
* and blank white screen cannot occur anymore.
