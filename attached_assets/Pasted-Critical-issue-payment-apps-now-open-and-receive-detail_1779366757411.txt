Critical issue: payment apps now open and receive details, but ALL apps (Paytm, GPay, PhonePe) show technical glitch and payment cannot proceed.

This is no longer a browser launch problem.

Screenshot evidence:

Restaurant name shown:
Olive Garden

Amount shown:
₹2.1

App opens successfully

But payment request fails internally.

Please debug and repair the UPI payload completely.

==================================================
HIGH PRIORITY CHECKS
====================

1. Log EXACT generated URL:

console.log("[UPI FINAL LINK]", upiLink)

Also log:

pa
pn
am
tn
tr
mc
cu

==================================================
EXPECTED FORMAT
===============

upi://pay?
pa=restaurant@okaxis
&pn=Olive%20Garden
&am=2.10
&cu=INR
&tr=BITEBN42
&tn=Order42

==================================================
AMOUNT FORMAT
=============

Current screenshot shows:

₹2.1

Potential issue.

Force:

const amount =
Number(orderTotal).toFixed(2)

Result:

2.10

Never:

2.1
₹2.1
2,10
undefined

==================================================
VERIFY pa
=========

Log:

UPI ID length
UPI ID exact value

Check for:

leading spaces
trailing spaces
hidden newline
double @
missing VPA suffix

Apply:

const upiId = upiId.trim()

==================================================
VERIFY UPI ID EXISTS
====================

Reject if:

!upiId.includes("@")

Show:

Invalid restaurant UPI ID

==================================================
REMOVE OPTIONAL FIELDS TEMPORARILY
==================================

Temporarily test MINIMAL payload:

upi://pay?
pa=<upi>
&pn=<name>
&am=<amount>
&cu=INR

Remove:

tr
tn
mc

Reason:

Some apps reject extra fields.

If minimal works:

Add fields back one by one.

==================================================
TEST MATRIX
===========

Test:

GPay
PhonePe
Paytm

Report:

exact generated URL
which field caused rejection
files changed

This needs a complete repair because payment currently cannot happen in any app.
