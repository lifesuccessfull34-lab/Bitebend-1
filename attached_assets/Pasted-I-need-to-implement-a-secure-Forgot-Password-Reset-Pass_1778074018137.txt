I need to implement a secure "Forgot Password / Reset Password" system ONLY for ADMIN users here "https://349b2a7f-8882-4ac0-92eb-683822c18341-00-19fdnunvedy62.sisko.replit.dev/portal/admin/login". 

IMPORTANT CLARIFICATION:
- DO NOT modify restaurant owner accounts, restaurant login, or any other user type.
- This feature must apply strictly to ADMIN authentication only.

CURRENT ISSUE:
- Admin users have no secure way to recover or reset password.
- Previous implementation incorrectly affected restaurant owner credentials — this must NOT happen again.

TASK:

1. Identify the ADMIN user model/table only.
   - Ensure separation from restaurant/owner user schema.
   - Do NOT touch restaurant-related authentication logic.

2. Add "Forgot Password" option ONLY on ADMIN login page.

3. Admin enters registered ADMIN email only.
   - Validate email exists in ADMIN table only.

4. Generate secure password reset token:
   - Valid for 15–30 minutes
   - Single-use only
   - Stored in ADMIN_RESET_TOKENS (or equivalent admin-only field/table)

5. Send reset link via email (Nodemailer or existing email service):
   - Format: /admin/reset-password?token=XXXX
   - IMPORTANT: route must be ADMIN-specific (/admin/*)

6. Create ADMIN Reset Password page:
   - Allow admin to set new password
   - Confirm password field

7. Validate:
   - Token is valid
   - Token not expired
   - Token belongs to ADMIN user only

8. Password security:
   - Use bcrypt hashing
   - Never store plain text passwords
   - Hash only ONCE during reset or creation

9. After successful reset:
   - Invalidate token immediately
   - Allow admin login with new password

10. Add safeguards:
   - Ensure no code path modifies restaurant owner passwords
   - Ensure no shared authentication logic between admin and restaurant users
   - Separate admin auth service/module if needed

11. Add logging for admin auth only (no sensitive password logs)

GOAL:
- Clean separation between ADMIN authentication and RESTAURANT OWNER authentication
- Secure, production-ready admin password recovery system
- No impact on any other user type