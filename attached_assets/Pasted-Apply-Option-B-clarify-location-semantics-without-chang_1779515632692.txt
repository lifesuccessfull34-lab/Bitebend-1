Apply Option B: clarify location semantics without changing backend structure.

We are NOT changing the DB or API contract — only improving clarity and preventing ambiguity in the frontend + spec.

---

## 1. Rename field semantics (frontend only)

In RestaurantAuth.tsx:

### Replace conceptual meaning:

* `district` → **District (administrative region)**
* `city` → **City / Town (free-text, independent of district)**

Important rule:

* City is NOT derived from district
* District and City are independent fields

---

## 2. UI label updates

Update form labels:

* District dropdown:
  "District (Administrative Region)"

* City input:
  "City / Town"

Add helper text under City:
"Enter the actual city or town name. This is independent of district."

---

## 3. State behavior (no structural change)

Keep existing logic, but enforce independence:

* Changing State:

  * reset district
  * reset city

* Changing District:

  * DO NOT modify city

* City remains free-text and unaffected by dropdown changes

---

## 4. Validation rules

Ensure:

* State optional/required (as currently implemented)
* District required only if state is selected (unchanged)
* City always required independently

Do NOT couple city validation to district.

---

## 5. OpenAPI clarification (important)

Update OpenAPI documentation ONLY (no backend change):

RegisterInput:

* restaurantState: string
* restaurantDistrict: string (administrative region)
* restaurantCity: string (independent locality, town or city)

Add description:

> city is independent of district and must not be inferred or derived from it

---

## 6. Data integrity rule (frontend enforcement only)

Ensure:

* No auto-filling city based on district
* No mapping assumptions between district → city

---

## 7. Files to update

* RestaurantAuth.tsx (UI + labels + validation)
* openapi.yaml (field descriptions only)
* Any shared form types if label constants exist

---

## Outcome

After this change:

* District = administrative boundary
* City = actual locality input
* No implicit coupling between them
* Same backend structure preserved
* RegisterPage.tsx remains unaffected

This resolves semantic ambiguity without schema migration.
