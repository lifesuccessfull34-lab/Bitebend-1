Need a temporary architecture change.

Pause automatic OCR verification entirely for now.

Reason:

OCR uses Google Vision API and adds cost.
Current stage does not require AI verification.

We already have:

Customer screenshot upload
Restaurant dashboard verification flow

That is enough.

==================================================

NEW FLOW

Customer:

QR payment
→ upload screenshot
→ continue

Do NOT run:

Google Vision OCR
OpenAI extraction
UTR extraction
auto verification

Disable all automatic payment processing.

==================================================

BACKEND

When screenshot uploaded:

Store image only.

Set:

paymentStatus:

awaiting_verification

verificationMethod:

null

Do NOT trigger:

GOOGLE_VISION_API_KEY flow
OCR worker
AI extraction pipeline

Keep code behind feature flag:

ENABLE_PAYMENT_OCR=false

Reason:

Easy future re-enable.

==================================================

RESTAURANT DASHBOARD

For orders with uploaded screenshots:

Show button:

Upload Screenshot

When clicked:

Open modal/page:

Payment Proof

Display:

uploaded screenshot image
order amount
customer name
order number
table number

Buttons:

Verify Payment
Reject / Close

==================================================

VERIFY PAYMENT

Restaurant visually checks screenshot.

If correct:

paymentStatus = paid

verificationMethod = manual_staff

verifiedBy = restaurantOwnerId

verifiedAt = timestamp

==================================================

CUSTOMER SIDE

After screenshot upload:

Show:

"Payment screenshot uploaded successfully."

and:

"Restaurant staff will verify your payment shortly."

Do not mention OCR.

==================================================

Keep all OCR code.

Do NOT delete.

Wrap behind:

ENABLE_PAYMENT_OCR=false

Future:

ENABLE_PAYMENT_OCR=true

can restore AI verification instantly.

==================================================

This is temporary cost optimization.

Manual verification becomes primary.
