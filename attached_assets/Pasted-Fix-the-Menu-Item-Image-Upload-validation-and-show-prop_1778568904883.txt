Fix the Menu Item Image Upload validation and show proper upload requirements to users.

Problem:
Dashboard → Menu → Add Item → Upload Photo shows upload failed without clearly telling:

* Allowed image types
* Maximum image size
* Validation reason

Please improve the complete upload validation UX.

==================================================
REQUIRED IMPROVEMENTS
=====================

1. SHOW CLEAR IMAGE REQUIREMENTS
   Under the upload section show:

Supported formats:

* JPG
* JPEG
* PNG
* WEBP

Maximum file size:

* 5 MB

Recommended:

* Square image preferred
* Recommended size: 800×800 px or higher

==================================================
2. FRONTEND VALIDATION
======================

Before upload starts:

Validate:

* File type
* File size
* Empty/corrupted file

If invalid:

* Show proper toast/error message
* Prevent upload request

Example errors:

* "Only JPG, PNG, WEBP images are allowed"
* "Image size must be below 5 MB"
* "Selected image is invalid"

==================================================
3. BACKEND VALIDATION
=====================

Also validate on backend:

* MIME type
* File extension
* File size limit

If invalid:
Return proper API errors instead of generic upload failed.

==================================================
4. BETTER ERROR HANDLING
========================

Replace generic:
"Upload Failed"

With meaningful errors such as:

* Unsupported image format
* Image too large
* Network upload failed
* Storage write failed
* Authentication expired

==================================================
5. IMAGE PREVIEW
================

Before saving menu item:

* Show selected image preview
* Allow removing/changing image

==================================================
6. DEBUG & FIX ROOT CAUSE
=========================

Investigate why upload is failing currently.

Check:

* Upload endpoint
* FormData request
* Storage path
* File permissions
* Multer/storage config
* Payload size limits
* API response handling
* Public image URL generation

==================================================
IMPORTANT
=========

* Do not break existing menu logic
* Keep existing database structure stable
* Ensure compatibility across desktop/mobile browsers
* Ensure uploaded images persist correctly

After fixing provide:

* Root cause
* Files changed
* Upload size limit implemented
* Allowed file types implemented
* Exact validation logic added
* How upload flow now works
