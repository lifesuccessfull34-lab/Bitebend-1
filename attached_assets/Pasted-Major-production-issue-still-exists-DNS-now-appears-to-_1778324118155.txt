Major production issue still exists.

DNS now appears to be partially working because bitebend.in opens, but the site only shows a blank white page.

This is now a FRONTEND BUILD/RUNTIME issue.

Symptoms:

* bitebend.in loads
* but page remains completely blank
* no UI renders
* QR-generated links also fail because frontend app is broken

This indicates:

* JavaScript bundle crash
* asset loading failure
* Vite base path issue
* React Router basename mismatch
* MIME/static serving issue
* production-only runtime error
* cached broken deployment
* service worker caching old assets

Please deeply inspect ALL of these:

1. Browser Console Errors
   Open production site and inspect:

* console errors
* failed JS loads
* MIME type errors
* CORS errors
* module loading failures
* React runtime errors

2. Network Tab
   Check:

* JS bundle status
* CSS bundle status
* chunk loading
* 404 assets
* wrong asset URLs
* incorrect /portal/ or /menu/ paths

3. Vite Build Configuration
   Inspect:

* vite.config.ts
* base paths
* asset URLs
* build output paths
* dynamic imports

Verify correct production bases:
Portal:
base: "/portal/"

Menu:
base: "/menu/"

4. React Router
   Check:

* BrowserRouter basename
* route configuration
* SPA rewrite handling
* artifact.toml rewrites

5. Service Worker / Cache
   Clear:

* service worker
* deployment cache
* browser cache
* static asset cache
* old build artifacts

6. Production Runtime Crash
   Check whether:

* app crashes during hydration
* undefined env variables
* production-only code path failure
* missing API response
* auth provider crash
* context/store initialization failure

7. IMPORTANT
   Perform a COMPLETE CLEAN rebuild and redeploy.
   Not incremental.

8. Final Verification
   After redeploy:

* bitebend.in/portal must fully render
* bitebend.in/menu/13 must fully render
* QR code must open correctly
* no blank page

9. Also provide:

* exact root cause
* files changed
* console error found
* confirmation tested on mobile browser

This is no longer a DNS issue.
The frontend production app itself is failing to render.
