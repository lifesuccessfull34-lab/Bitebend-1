Critical authentication issue on the published Bitebend public domain.

Problem:
When I enter the correct login credentials and click “Sign In”, the app briefly tries to log in but immediately redirects/bounces back to the sign-in page again instead of entering the dashboard.

This issue is happening ONLY on the public deployed domain, not necessarily in local/dev mode.

I need a deep inspection of the entire authentication/session flow.

Please carefully check ALL of these possible causes:

1. Session/Cookie Problems

* Cookies not being set on production domain
* secure: true causing issues on HTTP/HTTPS mismatch
* sameSite configuration incorrect
* domain mismatch
* cookie path issues
* session store resetting
* express-session misconfiguration

2. Production Environment Differences

* Environment variables missing in deployment
* SESSION_SECRET missing
* DATABASE_URL mismatch
* API URL mismatch
* frontend calling localhost APIs accidentally
* production build using wrong base URL

3. Authentication Flow

* Login API returning success but session not persisting
* Auth middleware rejecting authenticated users
* deserializeUser/session validation failing
* JWT token not stored/read properly
* Role-based redirect loop
* Router guard forcing redirect to /signin

4. Public Domain Deployment Issues

* Replit deployment proxy stripping cookies
* CORS settings incorrect
* credentials: 'include' missing in frontend fetch/axios
* backend not allowing credentials
* HTTPS cookie enforcement issue

5. Frontend Routing

* ProtectedRoute component redirecting incorrectly
* auth state resetting after login
* Zustand/Context auth store clearing
* React Query cache issue
* login success state not updating

6. Browser Console + Network Inspection
   Please inspect:

* Login request response
* Set-Cookie headers
* 401/403 errors
* Redirect loops
* Failed auth checks
* Console errors

7. Required Fix
   I need:

* Persistent login on the public domain
* Successful redirect to dashboard
* Session remains active after refresh
* Proper production-safe cookie/session configuration

IMPORTANT:
Do NOT apply temporary fixes.
Find the ROOT CAUSE and correct the production authentication architecture properly.

After fixing:

* Test login on the deployed public domain
* Verify refresh persistence
* Verify logout/login cycle
* Verify admin routing works correctly

Also print:

* Exact root cause found
* Files changed
* Final production auth configuration
