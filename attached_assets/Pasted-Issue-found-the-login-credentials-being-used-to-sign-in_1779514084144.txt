Issue found: the login credentials being used to sign into the Olive Garden account do not match the credentials displayed in Dashboard → Profile → Login Credentials.

Please do a full inspection only (no code changes yet) and provide an architectural diagnosis.

Investigate in parallel:

1. Authentication source of truth

* Trace exactly where Olive Garden login credentials are validated during login.
* Identify whether login uses database values, auth provider values, session values, environment variables, cached values, or another source.
* Confirm the exact table/collection and fields used.

2. Dashboard → Profile → Login Credentials source

* Trace where this page reads data from.
* Identify API route, backend handler, and DB query.
* Verify whether it reads from the same source as authentication or a different source.

3. Data synchronization flow

* Inspect how login credentials are created during restaurant onboarding.
* Inspect how updates to credentials are saved.
* Check whether changes update:

  * auth table
  * restaurant table
  * profile table
  * user table
  * session store
  * any duplicate credential store

4. Check for duplicate storage
   Search for duplicate fields such as:

* email
* username
* loginEmail
* login_credentials
* ownerEmail
* userEmail
* authEmail
* passwordHash
* credentials

Look for cases where credentials are stored in more than one location and may drift out of sync.

5. Session/cache inspection
   Check if:

* old credentials remain in session state
* stale Zustand/React state exists
* localStorage/sessionStorage keeps outdated values
* backend cache differs from database values

6. Olive Garden-specific verification
   Inspect the actual Olive Garden records and compare:

* credential used during login
* credential shown in dashboard profile
* database values
* auth values

Need a final report:
A. Actual login source of truth
B. Actual profile source of truth
C. Why mismatch happened
D. Exact files involved
E. Recommended fix without changing code yet

Do not modify anything until diagnosis is complete.
