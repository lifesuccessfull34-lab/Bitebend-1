I want to implement location-based filtering (State, District, City) for CUSTOMER DATA in the admin panel.

IMPORTANT:
This filtering must be applied to CUSTOMERS (derived from orders), NOT restaurants.

--------------------------------------------------

CURRENT ISSUE:

- Filters are incorrectly applied to restaurant data
- Customer list is not properly filtered by demographics
- No clear grouping of customers

--------------------------------------------------

PART 1: DATA SOURCE (CRITICAL)

- Customer data must come from ORDERS table
- Each customer is identified by PHONE NUMBER
- Do NOT use restaurant table for filtering customers

--------------------------------------------------

PART 2: CUSTOMER AGGREGATION LOGIC

1. Group all orders by phone number

2. For each customer calculate:
   - phone
   - name (if available)
   - totalOrders = count of orders
   - totalSpent = sum of order amount
   - lastOrderDate = latest order

3. For location fields:
   - state = most frequent state from customer's orders
   - district = most frequent district
   - city = most frequent city

IMPORTANT:
- These values must come from ORDER DATA (not restaurant table directly)
- Use restaurant location only as source stored inside each order

--------------------------------------------------

PART 3: FILTER IMPLEMENTATION

Apply filters on aggregated CUSTOMER LIST (after grouping), not raw orders.

Filters:

1. State Filter:
   - Dropdown of unique states from customer data

2. District Filter:
   - Dependent on selected state

3. City Filter:
   - Dependent on selected district

--------------------------------------------------

PART 4: FILTER LOGIC (IMPORTANT)

When user selects filters:

- Filter customers WHERE:
   customer.state == selectedState
   AND customer.district == selectedDistrict
   AND customer.city == selectedCity

- Do NOT filter restaurants
- Do NOT filter raw orders directly in UI

--------------------------------------------------

PART 5: ADMIN UI TABLE

Display filtered customer list:

Columns:
- Name / Phone
- State
- District
- City
- Total Orders
- Total Spent
- Last Order Date

--------------------------------------------------

PART 6: SEARCH + FILTER COMBINATION

- Search (phone/name) must work WITH filters
- Apply filters first, then search

--------------------------------------------------

PART 7: PERFORMANCE

- Perform grouping and aggregation in backend
- Send ready customer list to frontend

--------------------------------------------------

RULES:

- Do NOT mix restaurant filtering with customer filtering
- Use phone number as unique customer ID
- Do NOT show duplicate customers
- Apply filters AFTER aggregation
- Keep UI clean and responsive